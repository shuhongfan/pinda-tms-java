server:
  tomcat:
    uri-encoding: UTF-8
    max-threads: 1000
    min-spare-threads: 30
  port: 8186
  connection-timeout: 50000ms
#  servlet:
#    context-path: /pd-oms

spring:
  application:
    name: pd-oms
  # 环境 dev|test|prod
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true