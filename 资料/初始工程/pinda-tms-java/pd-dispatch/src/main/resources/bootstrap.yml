server:
  tomcat:
    uri-encoding: UTF-8
    max-threads: 1000
    min-spare-threads: 30
  port: 8190
  connection-timeout: 50000ms
#  servlet:
#    context-path: /pd-work

spring:
  application:
    name: pd-dispatch
  # 环境 dev|test|prod
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true





