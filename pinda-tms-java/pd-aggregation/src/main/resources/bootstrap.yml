server:
  tomcat:
    uri-encoding: UTF-8
    max-threads: 1000
    min-spare-threads: 30
  port: 8191
  connection-timeout: 50000ms
#  servlet:
#    context-path: /

spring:
  application:
    name: pd-aggregation
  # 环境 dev|test|prod
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true